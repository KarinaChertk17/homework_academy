#hw13
#ex1
s = input("Введите значеие").lower().replace(' ', '') #пользователь вводит значение, lower() — это строковый метод, который возвращает новую строку полностью без заглавных букв.replace -  это замена
if s == s[::-1]:#здесь у нас используется срез
   print('Это слово палиндром!')#и если слова палиндром, то выводится то,что это слово палиндром
else:
   print('Это слово не палиндром')#и если слова не палиндром, то выводится то,что это слово не палиндром

#ex2
text = input("Введите текст: ")
words = input("Введите слова: ").split() # split  для разделения строки на список строк
for word in words: #значения
    text = text.replace(word, word.upper()) # проиходит замента тест и аппер для того чтобы они стали заглавными буквами
print(text)

#ex3
text=int(input("Введите текст"))
for i in ("?", "!", "...", "."): #здесь мы смотри на все данные знаки
    text = text.replace(i,"|")#замена + подсчет
print(text.count("|"))
